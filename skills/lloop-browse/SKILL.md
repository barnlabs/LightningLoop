---
name: lloop-browse
description: Open one reputable page in the LightningLoop terminal or GUI browser, or a hash-verified local artifact.
---

# Browse

Use `llp browse URL` or `/browse URL` for a reputable HTTPS host. Use `artifact serve` for hash-verified local HTML on 127.0.0.1. Redirects, credentials, and non-reputable hosts fail closed.
